services:

  db:

    image: postgres:16-alpine

    environment:

      POSTGRES_USER: postgres

      POSTGRES_PASSWORD: postgres

      POSTGRES_DB: researion

    ports:

      - "5432:5432"

    volumes:

      - postgres_data:/var/lib/postgresql/data

    healthcheck:

      test: ["CMD-SHELL", "pg_isready -U postgres"]

      interval: 5s

      timeout: 5s

      retries: 10



  redis:

    image: redis:7-alpine

    ports:

      - "6379:6379"

    volumes:

      - redis_data:/data

    healthcheck:

      test: ["CMD", "redis-cli", "ping"]

      interval: 5s

      timeout: 5s

      retries: 10



  migrate:

    build:

      context: ./backend

    command: alembic upgrade head

    env_file:

      - ./backend/.env.example

    environment:

      DATABASE_URL: postgresql+asyncpg://postgres:postgres@db:5432/researion

    depends_on:

      db:

        condition: service_healthy

    restart: "no"



  backend:

    build:

      context: ./backend

    ports:

      - "8000:8000"

    env_file:

      - ./backend/.env.example

    environment:

      ENVIRONMENT: development

      DATABASE_URL: postgresql+asyncpg://postgres:postgres@db:5432/researion

      REDIS_URL: redis://redis:6379/0

      SEARCH_PROVIDER: mock

      TAVILY_MAX_RESULTS: 5

      ALLOW_SEARCH_FALLBACK: "true"

      AUTH_MODE: jwt

      AUTO_CREATE_TABLES: "false"

    healthcheck:

      test: ["CMD", "python", "scripts/docker_healthcheck.py", "ready"]

      interval: 15s

      timeout: 10s

      retries: 5

      start_period: 30s

    depends_on:

      db:

        condition: service_healthy

      redis:

        condition: service_healthy

      migrate:

        condition: service_completed_successfully

    restart: unless-stopped



  worker:

    build:

      context: ./backend

    command: arq app.workers.settings.WorkerSettings

    env_file:

      - ./backend/.env.example

    environment:

      ENVIRONMENT: development

      DATABASE_URL: postgresql+asyncpg://postgres:postgres@db:5432/researion

      REDIS_URL: redis://redis:6379/0

      SEARCH_PROVIDER: mock

      TAVILY_MAX_RESULTS: 5

      ALLOW_SEARCH_FALLBACK: "true"

      AUTH_MODE: jwt

      AUTO_CREATE_TABLES: "false"

    healthcheck:

      test: ["CMD", "python", "scripts/docker_healthcheck.py", "worker"]

      interval: 20s

      timeout: 10s

      retries: 5

      start_period: 20s

    depends_on:

      db:

        condition: service_healthy

      redis:

        condition: service_healthy

      migrate:

        condition: service_completed_successfully

    restart: unless-stopped



  frontend:

    build:

      context: ./frontend

      args:

        VITE_API_URL: http://localhost:8000/api

    ports:

      - "5173:80"

    depends_on:

      - backend

      - worker

    restart: unless-stopped



volumes:

  postgres_data:

  redis_data:

